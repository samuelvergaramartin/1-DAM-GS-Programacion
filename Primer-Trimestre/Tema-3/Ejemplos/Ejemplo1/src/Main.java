import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        while (mcd>0 && (a%mcd!=0 || b%mcd!=0)){
            mcd--;
        }

        while (!(mcd <=0 || (a%mcd==0 && b%mcd==0)))
    }
}